name: First Wokflow
on: workflow_dispatch
 jobs
  first-job:
    runs-on:macos-latest:
    steps:
      -name:Print greeting
       run:echo "hello world"
      -name:first goodbye
       run:echo "done-bye!"
