name: First Wokflow
on: Workflow_dispatch
 jobs
   first-job;
     runs-on:ubuntu-latest;
    steps;
      -name:Print greeting
       run:echo "hello world"
      -name:first goodbye
       run:echo "done-bye!"
